#Given a sorted array and a target value, return the index if the target is found. If not, return the index where it would be if it were inserted in order. There won't be duplicate values in the array.

#essentially a modified insert_sort with runtime O(log(2)) ...? insert_sort defined below
target_value <- function(vec, val){
  if(val %in% vec) which(val == vec)[1]
  i <- round(length(vec)/2)
  while(T){
    
  }  
}

#given a sorted array and a value, insert 
insert_sort <- function(vec,val){
  if(val < vec[1]) return(c())
  i <- round(length(vec)/2)
  
}